# Copy to .env and change the password. .env is gitignored.
# SQL Server requires >= 8 chars with three of: upper, lower, digit, symbol.
MSSQL_SA_PASSWORD=Sift_Dev_Local_2026!

# Host port for SQL Server. Change if 1433 is already in use.
MSSQL_PORT=1433
